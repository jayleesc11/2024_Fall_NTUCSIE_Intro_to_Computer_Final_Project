class Gate {
    // Gate types
    field int type; // {0:NULL 1:AND 2:NAND 3:OR 4:NOR 5:XOR 6:XNOR}
    // Position on the screen
    field int xPos;
    field int yPos;
    // Internal clocks
    field int tick;

    constructor Gate new(int itype) {
        
        return this;
    }

    method void reset() {}

    method void dispose() {
        do Memory.deAlloc(this);
        return;
    }

    method boolean output(boolean input) {}

    method void tick() {}

    method void draw() {}

    method void update() {
        do tick();
        do draw();
        return;
    }
}
