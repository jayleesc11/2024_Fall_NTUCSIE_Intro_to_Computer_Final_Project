class Obstacle {
    // Gates Library
    static Array gateLibrary;
    // Constants
    static int xVel;
    static int yGap;

    // Position on the screen
    field int xPos;
    field int yPos;
    // Internal clocks
    field int tick;
    // Logic gates
    field Array gates;

    function void init(int ixVel, int iyGap) {
        let xVel = ixVel;
        let yGap = iyGap;
        return;
    }

    constructor Obstacle new(int x) {

        return this;
    }

    method void reset(int x) {
        return;
    }

    method void dispose() {
        do gates.dispose();
        do Memory.deAlloc(this);
        return;
    }

    method int collision(int x, int y) {
        return 0;
    }

    method void draw() {
        return;
    }

    method void tick() {
        return;
    }

    method void update() {
        do tick();
        do draw();
        return;
    }

    method int x() { return xPos; }
    method int y() { return yPos; }
}