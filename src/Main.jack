
class Main {
    function void main() {
        var Game game;
        var boolean run;
        do Game.init();
        let game = Game.new();
        let run = true;
        while (run) {
            let run = game.run();
        }
        do game.dispose();
        return;
    }
}
